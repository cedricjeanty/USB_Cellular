# USB Gadget Settings
virtual_disk_path: "/dev/mmcblk0p3"  # DATA partition
mount_point: "/mnt/usb_logs"
outbox_dir: "/home/cedric/outbox"  # Local outbox for harvested files

# Modem Settings
modem:
  port: "/dev/ttyAMA0"
  baudrate: 115200
  timeout: 1
  apn: "hologram"
  chunk_size: 512000  # 512KB chunks for reliable transfer

# FTP Upload Settings
ftp:
  server: "eu-central-1.sftpcloud.io"
  port: 21
  username: "ee150b29745b44ccbed1050d03dfbb81"
  password: "j2xOLhBpftkVhbj2bfq8j8ApC3poSn5o"
  remote_path: "/"

# Timing and Logic Settings
quiet_window_seconds: 30    # Seconds of inactivity before harvesting
poll_interval: 30           # How often to check the disk status
upload_check_interval: 300  # How often to check for files to upload (5 minutes)
max_retries: 3              # Maximum retries for failed uploads
retry_delay: 10             # Delay between retries (seconds)

# Legacy settings (for compatibility)
upload_url: "http://your-server-ip.com/upload"
boot_upload_timeout: 180    # Seconds to attempt pending uploads on boot before USB mode  